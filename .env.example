# Base path - change this to your desired base directory
BASE_PATH=E:/RedditWorkflow

# Temp directory - change this to your desired temp directory (should not be on C: drive)
TEMP_PATH=E:/RedditWorkflow/temp

# Common directories (relative to BASE_PATH)
TEMP_DIR=temp
LOG_DIR=logs
CACHE_DIR=cache

# Video maker workflow directories (relative to BASE_PATH)
RAW_DIR=raw
CUT_DIR=cut
USED_DIR=used
FINAL_DIR=final
SUBTITLE_DIR=subtitle
AUDIO_DIR=audio
OVERLAY_DIR=overlay
